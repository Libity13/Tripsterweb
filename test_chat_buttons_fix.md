# 🧪 Test Chat Buttons Fix

## 🔧 **การแก้ไขที่ทำ:**

### **1. Backend (Edge Function) - แก้ไข AI Context:**
- ✅ **เพิ่มคำสั่งให้ AI รู้จัก** คำว่า "จัดเรียง", "คำนวณ", "แนะนำ" เป็น modification request
- ✅ **เพิ่มคำสั่งให้ AI ถาม** เมื่อผู้ใช้กดปุ่มโดยไม่ระบุรายละเอียด
- ✅ **ปรับปรุง CRITICAL INSTRUCTIONS** ให้ชัดเจนขึ้น

### **2. Frontend (Schema) - แก้ไข Validation Error:**
- ✅ **เพิ่ม `destinations` เป็น optional** ใน `REMOVE_DESTINATIONS` schema
- ✅ **แก้ไข ZodError** เกี่ยวกับ `destination_names` ที่ขาดหายไป

## 🧪 **วิธีทดสอบ:**

### **Test 1: Chat Buttons Context**
1. สร้างทริปใหม่: "อยากไปเที่ยวจังหวัดเลย 2 วัน"
2. ตอบคำถาม: "ไปกับครอบครัว, งบประมาณ 5000 บาท"
3. รอให้ AI สร้างแผน
4. **ทดสอบปุ่ม "เพิ่มสถานที่ใหม่":**
   - **Expected:** AI ควรถาม "กรุณาบอกชื่อสถานที่ที่ต้องการเพิ่ม"
   - **ไม่ควร:** AI ถามข้อมูลส่วนตัวซ้ำ

5. **ทดสอบปุ่ม "ลบสถานที่":**
   - **Expected:** AI ควรถาม "กรุณาบอกชื่อสถานที่ที่ต้องการลบ"
   - **ไม่ควร:** AI ถามข้อมูลส่วนตัวซ้ำ

6. **ทดสอบปุ่ม "จัดเรียงใหม่":**
   - **Expected:** AI ควรถาม "กรุณาบอกลำดับใหม่ที่ต้องการ"
   - **ไม่ควร:** AI ถามข้อมูลส่วนตัวซ้ำ

### **Test 2: Location Context Validation**
1. สร้างทริปใหม่: "อยากไปเที่ยวน่าน 3 วัน"
2. ตอบคำถาม: "ไปกับครอบครัว, งบประมาณ 5000 บาท"
3. **ตรวจสอบ:** AI ควรแนะนำเฉพาะสถานที่ในจังหวัดน่าน
4. **Expected:** ไม่ควรมีสถานที่จากจังหวัดอื่น (เช่น เชียงใหม่, ลำปาง)

### **Test 3: Modification Actions**
1. หลังจากสร้างแผนแล้ว
2. **ทดสอบ:** "ลบ น้ำตกศรีดิษฐ์"
3. **Expected:** AI ควรใช้ action `REMOVE_DESTINATIONS` โดยไม่ถามข้อมูลซ้ำ

4. **ทดสอบ:** "เพิ่ม วัดพระธาตุดอยสุเทพ"
5. **Expected:** AI ควรใช้ action `ADD_DESTINATIONS` โดยไม่ถามข้อมูลซ้ำ

## 📊 **Expected Results:**

### **✅ Success Indicators:**
- AI ไม่ถามข้อมูลซ้ำเมื่อกดปุ่ม
- AI ถามรายละเอียดที่เหมาะสมสำหรับแต่ละปุ่ม
- AI แนะนำสถานที่ในบริบทที่ถูกต้อง
- ไม่มี ZodError เมื่อใช้ปุ่มต่างๆ

### **❌ Failure Indicators:**
- AI ยังถาม "ไปกับใคร" เมื่อกดปุ่ม
- AI ใช้ `ASK_PERSONAL_INFO` สำหรับการแก้ไข
- AI แนะนำสถานที่จากจังหวัดอื่น
- มี ZodError เกี่ยวกับ `destination_names`

## 🔍 **Debug Tips:**

### **ตรวจสอบ Log:**
1. ดู AI response ใน console
2. ตรวจสอบ `actions` array
3. ดู `location_context` ใน AI response
4. ตรวจสอบ validation errors

### **หากยังมีปัญหา:**
1. ตรวจสอบ conversation history format
2. ปรับปรุง prompt เพิ่มเติม
3. ตรวจสอบ schema validation
4. ดู error messages ใน console

## 🎯 **การปรับปรุงเพิ่มเติม:**

### **Backend:**
- เพิ่ม validation logic หลัง AI response
- ตรวจสอบ hintAddress กับ location_context
- กรองสถานที่ที่ไม่อยู่ในบริบท

### **Frontend:**
- ปรับปรุง error handling สำหรับ validation
- เพิ่ม fallback สำหรับ schema errors
- จัดการ null coordinates ใน components อื่น
