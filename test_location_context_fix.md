# 🧪 Test Location Context Fix

## 🔧 **การแก้ไขที่ทำ:**

### **1. Backend (Edge Function) - แก้ไข AI Location Context:**
- ✅ เพิ่ม `CRITICAL: Location Context Validation` ใน system prompt
- ✅ สั่งให้ AI ตรวจสอบว่าแนะนำสถานที่ในบริบทที่ถูกต้อง
- ✅ ห้ามแนะนำสถานที่จากจังหวัดอื่น
- ✅ ถ้าไม่แน่ใจ ให้ไม่แนะนำเลย

### **2. Frontend (Chat.tsx) - แก้ไขพิกัด Default:**
- ✅ **เปลี่ยนจากพิกัดกรุงเทพฯ เป็น `null`** เมื่อหาไม่เจอ
- ✅ **ปรับปรุงการค้นหาพิกัด** ให้ลองหลายแบบ (tourist_attraction ก่อน, แล้วค่อยแบบทั่วไป)
- ✅ **ปรับปรุง searchQuery** ให้ฉลาดขึ้น (ใช้ hintAddress, เพิ่มจังหวัด)

## 🧪 **วิธีทดสอบ:**

### **Test 1: AI Location Context**
1. สร้างทริปใหม่: "อยากไปเที่ยวน่าน 3 วัน"
2. ตอบคำถาม: "ไปกับครอบครัว, งบประมาณ 5000 บาท"
3. **ตรวจสอบ:** AI ควรแนะนำเฉพาะสถานที่ในจังหวัดน่าน
4. **Expected:** ไม่ควรมีสถานที่จากจังหวัดอื่น (เช่น เชียงใหม่, ลำปาง)

### **Test 2: Coordinate Search Improvement**
1. สร้างทริปใหม่: "อยากไปเที่ยวขอนแก่น 2 วัน"
2. ตอบคำถาม: "ไปกับครอบครัว, งบประมาณ 5000 บาท"
3. **ตรวจสอบ:** ดูว่า "วัดหนองแวง" และ "ตลาดต้นตาล" มีพิกัดถูกต้องหรือไม่
4. **Expected:** ควรมีพิกัดจริงของขอนแก่น ไม่ใช่ `null`

### **Test 3: Null Coordinates Handling**
1. สร้างทริปใหม่: "อยากไปเที่ยวสถานที่แปลกๆ ที่ไม่มีจริง 2 วัน"
2. **ตรวจสอบ:** สถานที่ที่หาไม่เจอควรมีพิกัดเป็น `null`
3. **Expected:** ไม่ควรมีพิกัดกรุงเทพฯ เป็น default

## 📊 **Expected Results:**

### **✅ Success Indicators:**
- AI แนะนำสถานที่ในบริบทที่ถูกต้อง
- พิกัดสถานที่ถูกต้อง (ไม่ใช่ `null` หรือกรุงเทพฯ)
- `ItineraryPanel` แสดงรายละเอียดสถานที่ได้
- `MapView` ปักหมุดถูกที่

### **❌ Failure Indicators:**
- AI แนะนำสถานที่จากจังหวัดอื่น
- พิกัดยังเป็นกรุงเทพฯ หรือ `null`
- ไม่มี `place_id` สำหรับสถานที่
- `MapView` ปักหมุดผิดที่

## 🔍 **Debug Tips:**

### **ตรวจสอบ Log:**
1. ดู AI response ใน console
2. ตรวจสอบ `location_context` ใน AI response
3. ดู `searchQuery` ที่สร้างขึ้น
4. ตรวจสอบ `latitude`, `longitude` ใน database

### **หากยังมีปัญหา:**
1. ตรวจสอบ AI prompt เพิ่มเติม
2. ปรับปรุง searchQuery logic
3. ตรวจสอบ Google Places API key
4. ดู error messages ใน console

## 🎯 **การปรับปรุงเพิ่มเติม:**

### **Backend:**
- เพิ่ม validation logic หลัง AI response
- ตรวจสอบ hintAddress กับ location_context
- กรองสถานที่ที่ไม่อยู่ในบริบท

### **Frontend:**
- เพิ่มการลองค้นหาหลายแบบ
- ปรับปรุง searchQuery ให้ฉลาดขึ้น
- จัดการ null coordinates ใน components อื่น
