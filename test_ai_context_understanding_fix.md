# 🧪 Test AI Context Understanding Fix

## 🔧 **การแก้ไขที่ทำ:**

### **1. Frontend (Schema) - แก้ไข ZodError:**
- ✅ **เพิ่ม empty array case** สำหรับ `ADD_DESTINATIONS` เมื่อ AI ต้องการถามรายละเอียด
- ✅ **แก้ไข ZodError** เกี่ยวกับ array ที่ต้องมีอย่างน้อย 1 element

### **2. Backend (Edge Function) - แก้ไข AI Context Understanding:**
- ✅ **เพิ่มคำสั่งให้ AI รู้จัก** คำว่า "ช่วยหา", "หาที่พัก", "หาร้านอาหาร" เป็น modification request
- ✅ **เพิ่มคำสั่งให้ AI ใช้ RECOMMEND_PLACES** เมื่อผู้ใช้ขอหาที่พัก/ร้านอาหาร
- ✅ **ปรับปรุง CRITICAL INSTRUCTIONS** ให้ชัดเจนขึ้น

## 🧪 **วิธีทดสอบ:**

### **Test 1: AI Context Understanding**
1. สร้างทริปใหม่: "อยากไปเที่ยวหนองคาย 3 วัน"
2. ตอบคำถาม: "ไปกับเพื่อน, งบประมาณ 3500 บาทต่อคน"
3. รอให้ AI สร้างแผน
4. **ทดสอบ:** พิมพ์ "ช่วยหาที่พักและร้านอาหารด้วยได้ไหม"
5. **Expected:** AI ควรใช้ action `RECOMMEND_PLACES` กับ `place_types: ["lodging", "restaurant"]`
6. **ไม่ควร:** AI ถามข้อมูลส่วนตัวซ้ำ

### **Test 2: Empty Array Handling**
1. สร้างทริปใหม่: "อยากไปเที่ยวเชียงใหม่ 2 วัน"
2. ตอบคำถาม: "ไปกับครอบครัว, งบประมาณ 5000 บาท"
3. รอให้ AI สร้างแผน
4. **ทดสอบ:** พิมพ์ "เพิ่มสถานที่ใหม่"
5. **Expected:** AI ควรถาม "กรุณาบอกชื่อสถานที่ที่ต้องการเพิ่ม"
6. **ไม่ควร:** มี ZodError เกี่ยวกับ empty array

### **Test 3: Location Context Validation**
1. สร้างทริปใหม่: "อยากไปเที่ยวน่าน 3 วัน"
2. ตอบคำถาม: "ไปกับครอบครัว, งบประมาณ 5000 บาท"
3. **ตรวจสอบ:** AI ควรแนะนำเฉพาะสถานที่ในจังหวัดน่าน
4. **Expected:** ไม่ควรมีสถานที่จากจังหวัดอื่น

## 📊 **Expected Results:**

### **✅ Success Indicators:**
- AI เข้าใจบริบทการแก้ไขได้ดีขึ้น
- AI ใช้ action ที่เหมาะสมสำหรับแต่ละคำขอ
- ไม่มี ZodError เกี่ยวกับ empty array
- AI แนะนำสถานที่ในบริบทที่ถูกต้อง

### **❌ Failure Indicators:**
- AI ยังถามข้อมูลส่วนตัวซ้ำเมื่อแก้ไขแผน
- AI ใช้ `ASK_PERSONAL_INFO` สำหรับการแก้ไข
- มี ZodError เกี่ยวกับ array validation
- AI แนะนำสถานที่จากจังหวัดอื่น

## 🔍 **Debug Tips:**

### **ตรวจสอบ Log:**
1. ดู AI response ใน console
2. ตรวจสอบ `actions` array
3. ดู `location_context` ใน AI response
4. ตรวจสอบ validation errors

### **หากยังมีปัญหา:**
1. ตรวจสอบ conversation history format
2. ปรับปรุง prompt เพิ่มเติม
3. ตรวจสอบ schema validation
4. ดู error messages ใน console

## 🎯 **การปรับปรุงเพิ่มเติม:**

### **Backend:**
- เพิ่ม validation logic หลัง AI response
- ตรวจสอบ hintAddress กับ location_context
- กรองสถานที่ที่ไม่อยู่ในบริบท

### **Frontend:**
- ปรับปรุง error handling สำหรับ validation
- เพิ่ม fallback สำหรับ schema errors
- จัดการ null coordinates ใน components อื่น

## 💡 **ตัวอย่างการใช้งานที่ถูกต้อง:**

### **Scenario 1: ขอหาที่พัก**
- **User:** "ช่วยหาที่พักและร้านอาหารด้วยได้ไหม"
- **AI Response:** ใช้ `RECOMMEND_PLACES` กับ `place_types: ["lodging", "restaurant"]`

### **Scenario 2: ขอเพิ่มสถานที่**
- **User:** "เพิ่มสถานที่ใหม่"
- **AI Response:** ถาม "กรุณาบอกชื่อสถานที่ที่ต้องการเพิ่ม"

### **Scenario 3: ขอลบสถานที่**
- **User:** "ลบ น้ำตกศรีดิษฐ์"
- **AI Response:** ใช้ `REMOVE_DESTINATIONS` กับ `destination_names: ["น้ำตกศรีดิษฐ์"]`
